"""SQLite storage adapters.

The concrete store implementations live in :mod:`stores`; this package
exists so the assembly layer has a single place to import them from, and
so no business module imports SQL directly.
"""
